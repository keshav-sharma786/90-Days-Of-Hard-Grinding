import React, { useContext } from "react";
import { UserContext } from "./UserContext";

const Navbar = () => {
  return (
    <nav>
      {/* rest you just have to do the conditional rendering */}
      <h1>App</h1>
    </nav>
  );
};

export default Navbar;
