## server can include both hardware as well as the software portion.

> > Client Server Architecture => We have a client what does a client means, generally a client is referred to as someone who is accessing your server, see this server has some files, this server can have some HTML Files, this server can have some photos, this server can have some documents and a lot of other things this server can have,suppose the user who is sitting as the client and he has his computer and on that computer he has a browser,and he basically wants to request some data,Suppose he wants to access file on this server,How can he do that ??? Basically this client has to open a socket connection,this socket is a open connection, Suppose if i am accessing some website, i am writing www.namastedev.com, so i am making an api call, every client has an IP Address and every server also has an IP Address, client and server both are some computers, running their operating systems,but we call this as client, because it is requesting some data,so client is using a browser,you can call this client as a browser also,now we have a server.So if I want to access this file,So I will open a socket connection,and when I open a socket connection,and when i send a request over here,so somebody should be there to listen it.Somebody should be listening to those incoming requests,that application under server (hardware), is known as server sometimes,client is requesting some data from the server that means this client is making a request and this application is basically listening to that request,and according giving the files,Suppose if the user is requesting some image,so this application listens Ok Ok !! this user is requesting a image,so it will go to the image,fetch the image and give it back to the client.This is basically the Client Server Architecture.

## There can be multiple clients also, each and every client will make a socket connection,and will get the data,When the client will request something,it makes a socket connection, the server fetches the data,and gives it back,and then this socket connection is closed,now suppose if client has to make another request, so it makes another socket connection,once again server sends the data,it closes.So if there are so many users sitting on your website making a lot of api calls or may be requesting a lot of pages,that means they are opening the socket getting the data and the socket connection is closed and they are seeing the webpage on their website.

> > when this socket connection is being made,it basically uses a TCP/IP Protocol.When the data is being sent, it is sent using a TCP/IP Protocol.TCP stands for Transmission Control Protocol.It makes a TCP/IP Connection.What is IP ?? IP means Internet Protocol.We have an IP address for each of the computer.Do you know why we call it as a web ??? Suppose if there are so many people with their different computers,and all these computers are connected to the internet,so if all of them are connected to the internet,every computer will have an IP Address,basically a way to locate and specify that computer it is like an address of each and every computer. Now because all of them are on internet,so with the IP, with the unique IP, every computer will have a unique IP address,with that IP They can connect to any other IP,the other person can connect to any other IP.And everybody can connect to anyone.That is why it is known as Web.Anybody can connect to anyone.with internet the world is connected.This is why it is known as Web.In this big web we are just talking about one client and a server computer.

## Protocol => It is some set of rules which are defined between the computers to connect.Suppose if one computer wants to talk to the other computer.so they talk a similar language that is known as the computer protocol.ex => Suppose if i speak hindi,but hindi will be understood by that person only who knows hindi,if a person does'not understand hindi How will that person communicate with me ?So we have come to a common ground, some common rules Ok I will speak in english,you also speak in english and understand english,this whole series will continue in english,my native language is hindi, your native language can be kannada, urdu, malyalam etc,but we have agreed that okay we will talk in english that is when communication will happen.Similarly these protocols are like rules, these are set of rules, which the computers have decided okay,we will communicate using these rules.If you do'not communicate using these rules then no connection.You cannot talk to each other.

> > on the server, you might have heard of the HTTP Server, you might have heard of FTP Server,you might have heard of the SMTP Server,when a client makes a socket connection to a server, there are different protocols, there are different rules,there are different rules which the server will send their response in, so there are certain rules.these rules are known as http, ftp and smtp.http stands for hyper text transfer protocol,basically in which format,things will be sent to the client.FTP means file transfer protocol, suppose if we have a server which just serves the files so you use a file transfer protocol to send those files.SMTP stands for Simple Mail Transfer Protocol.When you are sending a file, suppose if you sending an email, so there is a specific format you need the data in, suppose if you are requesting a HTML Document so there is a particular format, so think of these protocols as different languages.Suppose this client and server have agreed to talk in terms of mail.this SMTP language will be used.Suppose if client and server wants to talk in some other languages.These are the rules and Protocols how the data will be sent.So whenever we talk about web server.Generally we talk about the http servers.http means hyper text transfer protocol.Suppose if you want to send simple text,suppose if you want to send some HTML,suppose if you want to send some JSON Data,suppose if you want to send some basic data,then use a http server.http is like a kind of language or rules that how the client will talk to the server.Generally when we create a server using node,we are basically creating a http server.we can create a FTP, SMTP servers also.Generally when we are creeating a web application,we are creating a web server and this web server is like a http server.

## whenever the client will request anything,this HTTP Server will handle it.This HTTP Server is just waiting for the incoming requests.there is no other job of server.The server is just waiting for these incoming requests,whenever the request comes,whatever the request is accordingly,it modifies the response,and give it back.Get the request, do some processing and give it back.Server is always active and the server is always waiting for the incoming requests.A server is always waiting for the socket connection.We create an http server that listens to the incoming requests.whenever you are requesting on http:// that means we are using http protocol.This is like of kind a language we are using to communicate with some server on the internet.

> > when you make a server request how the data is being sent ???data is not send directly in one go.Suppose if the client is requesting some file, server does'not take the whole file and transfer it.No it never happens like this.it happens in the smaller chunks and these smaller chunks are knowns as the packets.We are using the language http,but whenever we are sending the data, we are sending the data in the smaller chunks,we are sending the data in the smaller packets.So that means if i am requesting an html document, does it also comes in smaller chunks ??Because I see that the website loads very fast.I type google.com and hit enter and the web page loads, the data which came onto the browser,came in the chunks.small-small pieces, it does'not come directly,it comes in the small pieces, it is like a stream of data which flows.Stream of chunks.There is also a concept of streams and buffers in NodeJS.Stream means a connection, buffer means sending the data in smaller pieces and chunks.Acc to TCP/IP protocol, TCP/IP protocol says now you might be confused between TCP/IP, http, ftp and smtp protocol.The language that the client and server will speak is decided by http,ftp,smtp these are languages, TCP/IP is the protocol of sending the data over an IP when 2 ip's communicate,that is why it is known as TCP/IP Protocol.when the data is being transferred, it is sent in the form of packets.It is basically controlled by TCP/Ip.Transmission Control Protocol.In Node JS we have to write some code to send this data.Inside Node JS,there is a concept of streams And Buffers.

## We donot generally communicate using IP's,we communicate using domain names,whenever we make an request,we make the request to www.amazon.com,you are not talking to an IP.At the end of the day,everything maps to an IP.One address is the verbal address, If I say I live in Noida,I live in this and this street,this and this lane,my house number is this.This is the lane,this is basically my verbal address which you and I understand.But one more address is your Geolocation,kind of like a co-ordinates,kind of like a latitude and longitude co-ordinates.So think of this IP Address as a Latitude and Longitude co-ordinates and think of www.amazon.com as your verbal address.See computers understand and remember IP Addresses very well,but we as humans do'not understand these ip's.we will be very confused.Just like we are confused with the phone numbers.We donot remember phone numbers any ways,we have to save it with the name of somebody else.So we basically request the url www.amazon.com, but before making an api call to amazon.com,internally when you are requesting a website from your browser,it makes a call to the DNS Server,it means Domain name servers.It is kind of like a mapping between IP and your domain name.www.amazon.com is known as the domain name,you purchase a domain name right.DNS server basically manages this mapping between the domain name and the IP.This DNS server also reads this request one by one,first of all it reads okay it is www that means world wide web.then it will read amazon and then it will parse okay it is '.com', see 'amazon.in' can be very different,'amazon.co.in' can be very different.dns server reads it one by one and then it resolves the IP it basically gives the ip mapping and then when we have get the IP and then we request to this IP.Whenever in your browser you are typing www.amazon.com, behind the scenes there is a call which is being made to a dns server, that dns server is giving back the IP , now that IP call is made,and internally behind the scenes everything is being done.you as a user does'not happen to know what basically happens behind the scenes.When you request the IP, it goes to the amazon server, amazon.com server(hardware), inside that server (hardware) there is a http server which is handling your request,that http server gets back the video and gives back the video and the video that you are seeing,it coming in the form of smaller smaller chunks (packets).There is a stream of data which is being transferred and in stream there are buffers,you might have also heard of the word buffering.

> > In the server (hardware) there can be multiple applications(software aspect of server) running,can i create multiple http servers.I am creating a http server that means I am creating an application,can i create multiple applications on the same server (hardware) ??? the answer is yes, you can create as many servers as you want,you can create one http server you can create another http server also.Now suppose the users are requesting something,how do we get to know that to which server it should go to ??Which application, when i am creating 2 different http servers that means I am creating 2 different node applications.2 different node applications are running.2 different node http servers are running.How do we know that we have to go to this http server or another http server.that is defined by PORT.What is a port ?A Port is basically a number,a 4-digit number.We can say that our first http server uses the port 1010.Our second http server uses the port 3000.When i say i am requesting the hardware server IP (i.e 102.209.1.3: 3000),after colon (:), if i say 3000, so this 'IP + PORT' will tell me that I am referring the 2nd IP Server.Basically at the end of the day there is a computer,on that there is a operating system,on that operating system I have multiple servers/applications running in.There can be 1 application, there can be another application.There can be multiple applications.There can be multiple node http servers running at the same time.There can be multiple other applications also running,there is a mail application running,there can be a file application can also be running.there can be multiple small small servers inside the whole computer(server), that is also called a server.this whole big server (computer/hardware) can have multiple internal servers => mail server,file server,http server,and according to the port,it decides where to go, which place to go ??PORT is attached to one server.We can have multiple node applications running on the same server,I can multiple http servers together over here,but I would need a different PORT Number.why the PORT number is needed??PORT Number is needed so that other people can connect to your application.this one http server(node application) is assigned one port.Can I use any 4-digit number for the port ??see there is a PORT 80 also.so it is not restricted to 4-digit or something, but we generally use port in 4-digits and generally we use this port 3000, react applications generally uses a 3000 port.There are some port numbers which are basically reserved for the system,so you cannot use that.

## Domain name maps to an IP,suppose if the domain name is amazon.com, so it gives you an IP Address.When you get the IP and the PORT together,it tells you which node application you have to go to ?? and if you give it a path,then it is associated to an api.

**Domain Name -> IP + PORT + PATH->API**

## suppose if you request amazon.com,this amazon.com is a domain name, it maps to an IP,let us call this IP Address as 123.4.5.6,this is an IP Address,now suppose I am having a port of 3000 over here,so 123.4.5.6:3000. This 3000 port will tell me that okay I am referring to my http server.Now suppose if i give it some path,like amazon.com/api/.this /api maps to the node application.Now if i say amazon.com/api/getUserInfo.This is my path,this path will decide that inside my server what code will run.Whenever there is a domain name amazon.com,it has an ip 123.4.5.6,and now we need to identify the port also.123.4.5.6/api, this /api is associated with PORT 3000.suppose if just do amazon.com/ and now you are referring to amazon.com/api/..... you can write logic to map this amazon.com/api to port 3000 so if in the url you find /api,you will refer to the port 3000,now suppose you are not putting anything and you are just referring to the amazon.com/, you can map this "/" to some other port also.

> > suppose you have a react application,which gives back you the html and website.Now there is a node application also.I want if somebody requests,amazon.com/, so it should go to my react application,this react application can be running on 3000 port number,but now i want whenever somebody requests amazon.com/api/...anything.I want the user to go to the port no.3001,so basically this amazon.com/api/... is mapped to 3001 port and amazon.com/ is mapped to the 3000 port.So basically whenever there is "/" and there is no api,it will go to the React,and if there is /api, it will go to Node.you might have heard that behind the scenes,there are so many different applications running,but everything is running on namastedev.com only,the same domain name because the domain name namastedev.com is just referring to this ip 123.4.5.6,but behind the scenes there can be multiple other servers being managed.

## when i will be building the project,so I will be building the front end also in my computer, I will be building the backend also in my computer, my database will also be in the same computer,I have everything on my same computer, on the same system,you can do it, but in the lot of cases,when there is a big company,so what happens is they donot deploy their whole application onto a single sever (hardware), here when I am talking about server I am talking about one computer.They donot deploy everthing onto a single server, single computer.They keep their database onto some other server(computer),they can also have a different file server (computer),or a server where you just have files.One computer just have the dump of files.There is one computer which just have the dump of images or there is a CDN also.there is a server which is only effecient for handling the images.When a client is making a socket connection and the client is requesting some data,sometimes the http server(application/software aspect of server) will get the data from within the hardware server and responds back, sometimes this server this application,do'not have to go anywhere and gets the data within the hardware server and responds back.But sometimes according to the client's request,it can also make an api call or it can also make an connection request with some other external server(hardware),suppose it makes a connection to the DB Server, it gets back the data from the database and sends it back to the client.so the application server can talk to other hardware servers as well because it's all web, multiple computers can talk to each other through protocols and rules which are defined by the web.


>> When you go to the namastedev.com,it is served by the server which we have got from AWS.this namastedev.com website is being served by the AWS server,the frontend is on the same server,the NodeJS Application that is getting the data that http server is also on the same server,but our database is on some other server (hardware/computer),we have not kept our database and the Node JS Application on the same server.we have our frontend and backend application on the same server,the namastedev.com that you browse and namastedev.com/api is coming from one server(computer/hardware),our database is on other server(computer/hardware).This database server is very effecient,it takes the backup of database,this database server is very powerful and it is just managing the database.It is very fast.If I keep the database, frontend,backend everything on same server(computer/hardware),then it becomes a problem.In this Namastedev.com,our frontend and backend are running on the same server on the same machine,our database is on some another server,another machine,When you request a video,these videos are coming from some another server(computer/hardware) some another machine,internally and then it comes to you, you donot get to know.The images that are coming to you, that is also not on our AWS server,these images are hosted on some another server(computer/hardware) machine.In big companies even the frontend and backend that we have kept on the same server are kept in the different servers (computers/hardwares).


## Sockets V/S Web Sockets ==> There are multiple users who can come in to your server and request some data,url,files,html,json.In general case whenever you load some website,suppose if you are loading Namastedev.com,our server will make an socket connection,gets back the response of Namastedev.com,that namastedev.com html will be rendered onto your browser and this socket is closed.After sometime you went to some other episode of this series,if you go to some another page,now you have to make another request to the server,you again open a socket,you get the data back you close the socket.This all is being handled by our http server.

>>web socket is a new concept, it means if the user has made a connection,and this connection stays for a longer time and it is not closing automatically,till the connection is made,this connection is not being closed between the client and the server,but it holds over there.Opening a web socket consumes a lot of resources also,and the server also has the capacity.


