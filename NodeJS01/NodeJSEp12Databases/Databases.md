## What is a database ? => It is a place where we store our data, not a good answer. A Database is basically an organised collection of data.The word organised is very important over here,database means you are organising the data in a way so that you can perform certain operations very easily.You can basically add data in that structured format/organised format.A database is a structured as well as the organised collection of the data.

> > what is the difference between a database and DBMS [Database Management System] ??there is a place where we store data in a structured way.That is basically known as a database,but on top of it,there is also a layer of database management system. Database Management system is a way that I can interact with that data.Suppose if i want to add more data,if i want to get some data out of it,you need a layer,so there is a layer of database management system,so that is the way by which users can talk to that database because database is just a data store right.We are just storing some data in a organised way, but to fetch data effeciently,to put data effeciently into the database,you basically need a Management system.That Management System is known as the Database Management System [DBMS].

## There are multiple types of database => Majority of the companies use Relational Database(MySQL and PostgreSQL) and NoSQL Database => MongoDB.These are the 2 most widely used type of databases.But What is the use of having so many types of databases ?? Every database has it's own core purpose.You can store data in multiple ways for ex => There are multiple types of Data Structures,there is array,linked lists,trees,graphs.Even inside Tress there are different types of trees,there is a Red Black Tree,there is a Binary Tree,there is a Binary Search Tree,there is a heap.There are basically so many types of Data Structures.Each and every data structure has it's own pros and cons.Similarly in databases also,there are different types of Databases.Each of the database has it's own pros and cons.Some databases are very good at storing the Transactions.Some databases are read-heavy database.Some databases are write-heavy database.Different types of databases are there.It depends on the use case,What do you want to do with that data.

> > MongoDB database is widely used with Node JS. Relational Databases RDBMS (MySQL, PostgreSQL) => In 1970's and 1980's,the wave of the relational databases started.There was a famous Mathematician scientist.His name was EF Codd.He introduced a concept of Codd's 12 Rules.But the rules were 13 because his numbered his rules from 0 to 12.Because He was from the computers and zero indexing was in his head.That's why he named the rules from 0 to 10.12 was the last number that's why people started calling it as Codd's 12 rules.Basically he designed these rules to tell that if your database follows these 12 rules,it basically becomes a relational database.These 12 rules were applicable to RDB i.e Relational Database.Relational Database means that you have different tables,and you have relation between these tables,so basically the data-model is relational.In 1980's relational databases gained a lot of popularity.In RDBMS also there are so many databases which are RDBMS.But the 2 most databases are MySQL and PostgreSQL.

## MySQL => MySQL was invented by the scientist called Michael Widenius.He was a computer engineer who basically created MySQL.Michael Widenius was having 3 daughters.First daughter name was My.2nd daughter name was Max.3rd daughter name was Maria.'My' was the eldest daughter,so he named MySQL database after her name.He also created one more database,which was majorly for enterprises,but he named it as Max DB.This Max DB name came after the name of her second daughter.And He also created MariaDB which was basically named after her 3rd daughter.MariaDB is basically a fork of MySQL.It is basically a clone of MySQL why?? ==> MySQL was acquired by the company known as Sun MicroSystem.An Year SunMicroSystem was acquired by the Oracle.Currently MySQL database is acquired by Oracle company.So when this acquired happened,so Michael and the core team of MySQL they created a fork and that fork was known as MariaDB.Right now MySQL database is being managed by the Oracle company.

> > PostgreSQL ==> There was a scientist whose name coincidently was also Michael StoneBreaker.This guy was working on a project Ingres.This project was the part of University Of California.It is a very famous University is Berkley.He started building the project Ingres.Later on he left the University and then He again came back to the University and started a new project called Post Ingres.And the project was of SQL,SQL means Structured Query Language.All the Relational Databases RDBMS uses SQL.He was creating a relational database and it was PostIngres and SQL so the name became PostgreSQL.PostgreSQL is also very famous,a lot of companies who want to work on relational RDBMS,they use PostgreSQL on production.

## SQL means Structured Query Language.What is a database??A database is an organised collection of data,you basically have a place where data is stored in an organised and in a structured fashion.You need a way to interact with that database also,you need to put some data into it,get some data out of it,you need a database management system,so when you are putting data into it or you are getting data out of it,these things are known as queries,so you make a query getQuery, postQuery.So you basically need a Query Language to interact with that database,that is basically a structured query language.Structured Query language is used in RDBMS.

> > NoSQL [MongoDB] ==> There are multiple types of NoSQL database also.There are basically 4 types of NoSQL Database ==> 1.) Document DB 2.) Key value DB 3.) Graph DB 3.) Wide Column DB.There is more type which is known as multi model.Multi model means that it comprises of multiple models.Out of these MongoDB is a document database.MongoDB uses documents.NoSQL means that it is not SQL.The People who have worked with NoSQL database often call it as not only SQL.It is much more than that.The wave of NoSQL Databases started in late 2000's.Earlier before that RDBMS was very very popular.MongoDB was created in year 2009.NodeJS was also released at the same time in 2009.NodeJS and MongoDB go hand in hand.It looks like NodeJS and MongoDB are made for each other.MongoDB was developed by the company which is known as 10gen.This word 'Mongo' came from the word Humoungous which means gigantic,huge,large.MongoDB was supposed to hold huge amount of data.And Yes MongoDB can store the huge amount of data and that too unstructured data.MongoDB can scale horizontally as well as vertically also.Later on 10gen started fully working on MongoDB and they also changed the name of their company to MongoDB inc.This is the company which basically manages mongoDB.MongoDB is very flexible and it is also very much developer friendly.MongoDB is very much compatible with NodeJS.MongoDB is built using C++.It has some javascript code,It also has some c++ code.It also has some python code.At the end of the day,everything is code only.Database is also set of prewritten code written by the other developers.A Database is also built using some lower level languages like c++, Javascript, pyhton.There are some people who are written that lower language code which interacts with the computer,very low langauge code.But at the end of the day it's code,

## In relational databases,data is stored in the form of tables.Every table has rows and columns.User table stores the information about the user.Now you want to store the hobbies of the users also.There are 2 ways to store the hobbies,either I can create the new column over here.

## Difference between RDBMS and NoSQL database ==> https://excalidraw.com/#json=NHuo3OmdhDJtmFSvy73_-,gOgW36RM9u3v-KiafO3_9A.

**In RDBMS we have a fixed schema,schema means what this table will contain, this table will contain a first_name,last_name and an id,this schema is basically fixed.You cannot change it very often.Changing the schema is a very big headache inside the RDBMS.Changing the schema is not very flexible in RDBMS.But NoSQL database have the flexible schema.**

## RDBMS always uses a Structured Query Language.Always use a notebook and a pen for taking down notes.In NoSQL database Query language also changes according to the types of NoSQL databases.In MongoDB we have MQL which stands for the Mongo Query Language.In Neo4J,the query language is known as Cypher.

## In RDBMS, horizonatal scaling is very tough,if you want to scale you database horizontally,it scales vertically properly,but it is a little tough to scale the data horizontally.But the mongoDB is easy to scale horizontally as well as vertically,because it is designed and structured in that way.

**_In RDBMS there are relationships using foreign keys and joins,but in MongoDB, you do'not have keys and joins.in NoSQL database you can keep nested data like we created hobbies array itself in the document._**

\***\*RDBMS is good for Read-heavy applications and suppose if you are having transaction work loads,NoSQL database is very good for the real time data, big data, distributed computing, this distributed computing is a very important use-case of the NoSQL databases.Distributed computing is very tough using MySQL.\*\***

**_RDBMS is generally used in the Banking apps because banking apps has transactions, suppose if you want to build some analytics dashboard,or some real time dashboards,if you want to make some social media kind of application, but social media is also very big, in which part, NoSQL databas e is good ,it is good to have NoSQL database._**

## Read about Uber Schemaless Database.
